# empeeku runtime snapshot

This directory contains only the request-buffer MPQ reading path from
`empeeku@1.0.2`, rewritten to TypeScript and patched with strict archive,
table, block, sector, compression, per-member, and cumulative-output bounds.
Filesystem extraction and command-line helpers are intentionally omitted.
